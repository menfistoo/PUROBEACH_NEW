# =============================================================================
# PuroBeach Production Environment Configuration
# =============================================================================
# Copy this file to .env.production and fill in all values.
# NEVER commit .env.production to version control.

# --- Required ---

# Secret key for session and CSRF (min 32 chars).
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# Database path inside the container (mapped to db-data volume)
DATABASE_PATH=/app/instance/beach_club.db

# --- Domain & SSL ---

# Public domain (used by Nginx and Certbot)
DOMAIN=example.com

# Email for Let's Encrypt certificate notifications
ADMIN_EMAIL=admin@example.com

# --- Optional ---

# Session timeout in hours (default: 8)
# SESSION_TIMEOUT_HOURS=8

# Max upload size in bytes (default: 16MB)
# MAX_CONTENT_LENGTH=16777216

# Upload folder path inside the container
# UPLOAD_FOLDER=/app/static/uploads
